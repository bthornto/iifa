<p id="notice"><%= notice %></p>

<dl class="dl-horizontal">
  <dt>Date:</dt>
  <dd><%= @cycle.date %></dd>

  <dt>User:</dt>
  <dd><%= @cycle.user.name if @cycle.user %></dd>

  <dt>Flavor:</dt>
  <dd><%= @cycle.flavor.name if @cycle.flavor %></dd>

  <dt>Size:</dt>
  <dd><%= @cycle.size.size if @cycle.size %></dd>

  <dt>Quantity:</dt>
  <dd><%= @cycle.quantity %></dd>

  <dt>Defect type:</dt>
  <dd><%= @cycle.defect_types.map { |x| x.name }.join(" ") %></dd>

  <dt>Cavity number:</dt>
  <dd><%= @cycle.cavity_number %></dd>

  <dt>Job number:</dt>
  <dd><%= @cycle.job_number %></dd>

  <dt>Shift:</dt>
  <dd><%= @cycle.shift %></dd>

  <dt>Location:</dt>
  <dd><%= @cycle.location.name if @cycle.location %></dd>
</dl>

<%= link_to 'Edit', edit_cycle_path(@cycle) %> |
<%= link_to 'Back', cycles_path %>
